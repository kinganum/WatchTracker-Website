
import { ItemType, SubType, Status, Language, ReleaseType } from './types';

export const TYPE_OPTIONS = Object.values(ItemType);
export const SUB_TYPE_OPTIONS = Object.values(SubType);
export const STATUS_OPTIONS = Object.values(Status);
export const LANGUAGE_OPTIONS = Object.values(Language);
export const RELEASE_TYPE_OPTIONS = Object.values(ReleaseType);
